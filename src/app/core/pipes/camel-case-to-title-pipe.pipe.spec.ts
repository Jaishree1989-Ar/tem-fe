import { CamelCaseToTitlePipePipe } from './camel-case-to-title-pipe.pipe';

describe('CamelCaseToTitlePipePipe', () => {
  it('create an instance', () => {
    const pipe = new CamelCaseToTitlePipePipe();
    expect(pipe).toBeTruthy();
  });
});
